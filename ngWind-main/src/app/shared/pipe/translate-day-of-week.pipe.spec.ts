import { TranslateDayOfWeekPipe } from './translate-day-of-week.pipe';

describe('TranslateDayOfWeekPipe', () => {
  it('create an instance', () => {
    const pipe = new TranslateDayOfWeekPipe();
    expect(pipe).toBeTruthy();
  });
});
